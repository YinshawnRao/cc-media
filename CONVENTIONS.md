# CONVENTIONS.md — 制作与编辑规范

本文件维护制作默认和编辑验收目标。代理边界见 [AGENTS.md](AGENTS.md)，可执行步骤与 manifest 字段见 [Runbook](tools/video/README.md)。用户当期要求可覆盖制作偏好；技术门禁不因审美选择降级。

## 配音

当前新旁白统一使用中央 `tools/tts/narrate.py` 的 **Qwen3-TTS** 能力，消费预先制作的参考声音。声音池、编号与决策画像分别以 `tools/tts/config.json`、`tools/tts/voices/registry.json` 为真源，不在规范重复列举。唯一精确指定优先；未唯一指定时由模型按作品情绪、叙事和节奏选择，低置信度才一次随机兜底。同一期选择固定，生成、检查和恢复见 [TTS README](tools/tts/README.md)。

只在生成新旁白时检查 TTS；复用历史 WAV 不自动换声。Kokoro 仅属显式历史复现路径，不是当前默认或故障降级方案。

## 素材与画幅

- 默认从 YouTube 和 B站各查候选，先核实目标歌手和表演版本。翻唱素材优先对应翻唱者，不用原唱画面冒充；真实缺口需记录原因。
- 身份正确后优先可用官方 MV，低清本身不是弃用理由。无可用官方 MV 才比较官方 Live、综艺或二次来源；同级比较干净度、立体声和画质。
- 用户明确指定链接、排除平台或素材确属平台独占时可免重复搜索。manifest 显式记录 search_exception，保留真实候选和选择证据。已有证据可复用，但不能把过期可用性当作当前事实。
- 成片固定竖屏 **1080×1920（9:16）**；只有用户明确要求其他画幅才调整，并在 manifest.output_format 记录尺寸与用户原话。不能仅凭 design 中的“保留全景”“更有电影感”自行改成横屏。展示画面默认全宽保原比例、模糊填充（letterbox）；复用 `vfill.sh`。横屏来源仍置于竖屏画布内。只裁水印/烧词的全宽窄横带。用户要求其他构图时检查完整候选窗，保证主体完整，多人不得被裁掉。
- 成片不出现平台/UP主水印、网址、提示词或本地路径。无法裁净时更换干净来源。

## 结构、旁白与音乐

TOP 默认按 N→1 逐首揭晓；封面和 intro 不泄第一名、完整歌单或内部倒数机制。非排名按叙事顺序，不挂 TOP 名义。信息标识避免同义重复，展示段默认淡出排名/歌名浮层。

默认结构是开头、逐首短转场、作品收束和 CTA。需要不同结构时在 manifest 的 editorial 中声明选择和 brief/design 理由，不伪装成自由探索。intro 建立主题，outro 收束；转场优先短而具体，句数与判断数量由文案决定。开头直接进入主题，避免空泛串场语，不设禁词表。

新项目 schema v2 的转场 WAV 仍执行：TOP 硬上限 8 秒（目标 4–6），叙事硬上限 10 秒（目标 6–8）。超限先精简旁白，不靠延长歌曲刷比例。其他自定义段按实际结构组织，不能改 role 规避真实歌曲转场的时长检查。

CTA 执行下文[结尾引流配音](#结尾引流配音)要求。章节时长根据实际 WAV 和乐句计算，不照抄旧常量。

音乐展示先选完整段落，再计算时长。“短视频”只描述定位，不隐含 3–4 分钟或任何总长上限，也不按曲数平均分配秒数。每首保留完整连续副歌或代表性核心段落，必要时连同铺垫、回应句、第二轮副歌或器乐发展一起保留；一句唱完不等于整个段落已经完整。初步找窗可先预留约 60 秒的纯音乐展示空间，再按作品伸缩；这不是硬下限或上限，完整短段可以更短，长段超过一分钟也不因此缩短。展示时长不包含旁白和 duck 时间，不用加长口播填总时长。

取材时在拟选核心段前后各保留至少 3–5 秒可回调素材（源边界不足时如实记录），先核对带上下文的候选窗，再确定成片切点。满音量展示通常从首个目标唱字/音符前约 1–2 秒开始，末个唱字、长音与混响实际结束后再留约 1–3 秒音乐余韵；按真实节奏调整，不能为了凑缓冲拖进下一句后再切断，也不填人工静音。旁白应在核心入点之前收完，full-music 展示开始后没有旁白。淡出放在完整尾音之后，不能用淡出掩盖半句截断。纯音乐同样保留完整主题或乐段的发展与解决。

逐首在 design/选段表记录核心段源起止、最终展示起止及选择理由。试听入点前后、出点前后至少各 5 秒上下文；mux 后再听每个实际交接，核对首字、尾音、淡出和下一首衔接。自动边界检测只证明它覆盖的证据，不能把低能量气口当作完整段落证明。有用户硬时长时先精简重复信息，仍无法兼顾完整段落与锁定歌单才询问取舍，不强截音乐。

旁白期间音乐 duck，留适当消化位再恢复全量。通常以约 25% 音乐增益和 0.8–1.2 秒过渡为起点，按实际听感调整。预混 `master.wav` 后期 mux，最终响度、true peak 和静音以中央 FINAL 为准；不铺噪声或无关音乐绕过检测。

## 结尾引流配音

- **必须保留**：有旁白的盘点、解说与主题叙事成片，在作品内容收束后追加一句简短的引流配音，明确邀请观众点赞、收藏、关注。它是全片最后一句旁白，只出现一次；brief 未提及仍须自动补齐。主题问句只能作为可选的内容互动，不能替代引流句，也不必每期强行提问。
- **统一短句**：新项目逐字使用 `tools/video/outro_cta.py::FIXED_OUTRO_CTA`（`short-v1`），通过 `python3 tools/video/outro_cta.py` 取得。只保留一句自然邀请，不附加投票问题、长篇下期预告或催促话术；自然语速目标约 3–6 秒，以实际 WAV 为准，不加速挤时长。
- **例外由用户决定**：只有用户明确要求替换、省略 CTA 或全片无旁白时才调整，在 `editorial.cta_user_request` 记录用户原话并填写 reason。模型不得以“非排名”“纪录片气质”“去模板化”“避免通用话术”或已有主题问句自行取消。真实自由探索与 AI 音色 MV 仍按各自声音方案，不得借项目类型规避普通叙事的要求。
- **实际入片**：CTA 与本期共用已解析声线，独立登记 text/WAV/sidecar，作为最后一条 `outro_cta` 绑定时间轴；音乐适当 duck，念完保留自然收尾，不截尾字。可配简洁引导浮层，但画面文字、发布文案和静音 WAV 都不能代替引流配音。
- **验收闭环**：PROJECT 校验 CTA 文本、唯一末位及真实 WAV/sidecar；FINAL 继续检查最终 MP4 的音轨绑定和对应 ASR 覆盖，并实际核对片尾完整、清晰可听。修改片尾须重建受影响的旁白、音轨、画面及 mux，刷新当前成片 SHA 的 QA。历史长版仅供已有项目按 Runbook 显式固定版本后复现，新制作使用短版。

## 字幕与视觉

默认不添加自定义旁白同步字幕；用户明确要求字幕才添加。信息卡、歌名、主题和 CTA 浮层是设计信息，避免逐字复制口播。

在 design.md 简述主题、风格和关键取舍即可；配色、字体、布局、装饰数量、easing、镜头切换方式由模型根据作品决定。不要求暗底、三种 easing、8–10 个元素、持续装饰运动、纯色禁令或强制 prompt 扩写。Flex、Grid、绝对定位和手动语义换行均可，按实际画面验收。

- 第 0 帧完整可读，可直接作封面；首屏文字不从透明开始。后续是否入场/退场/硬切按内容选择。
- 第一首动态素材连续流入正文是可选优先方案，也可以使用更合适的静帧或独立封面，不要求证明其他方案穷尽。
- 单歌手主题须让歌手身份突出；不要求其字号严格大于所有其他文字。按主题与负空间确定视觉焦点。
- 语义自然断行，不留下孤字、不挡脸或主要动作。关键文字集中在安全区域；1080×1920 可参考 x=72–1008、y=220–1420，其他画幅按目标裁剪重新设计。
- 封面已有“最难”等明确榜单语义时，默认省略重复 TOP 角标。其余信息去重按实际画面判断。
- 以首帧、代表时刻和目标平台裁剪预览检查完整可读、主体避让、色彩与层级；元素数量和 CSS 写法不是质量证明。
- 新项目使用项目内 WOFF2。历史本机专用工程可继续已记录的 `local()`，但不能据此宣称跨机可复现。渲染不依赖远程字体请求。

## 发布文案

默认随片交付 publishing/xiaohongshu.md，结合真实主题、事实与本期角度写成可直接发布的内容。可用已有来源研究，不为补文案机械重复调研。

标题候选 1–5 个，默认 3 个，第一条首选；正文最后一行放相关且不重复的 hashtags。420–900 字、8–10 个标签是参考范围，不是硬门禁；段落和互动问题按内容决定，不凑字数和标签。默认不用 emoji。

避免明确曝光本期歌名；《歌名》和精确歌名 hashtag 属明确引用。普通词语恰好与歌名相同（如“后来才发现”）由编辑看上下文，不直接判剧透。校验器将歧义和长度提示为 EDITORIAL，不证明文案事实或审美已通过。

## QA 与恢复

构建前 PROJECT 验证来源、当前片段、旁白及展示证据，mux 后 PUBLISHING 和 FINAL 验证交付。不能用自报 PASS 代替中央工具。技术细节集中在 [Runbook](tools/video/README.md) 和 [运行与恢复](tools/video/operations.md)。

展示检测先使用 multi。REVIEW 按原因处理：可修复识别问题则重跑分析；窗口有问题则换窗；素材有问题则换同版本源。Live 分类器本身不能自动 OK 时，不要求重复尝试必定 REVIEW 的窗口；结合原源、歌词、口型和音频证据作当前 hash/窗口绑定的 agent observation，状态只能是 OBSERVED。硬 FAIL 必须修复。发布模式只接受真实 human 批准。

标准 FINAL 中 >1.5 秒连续静音始终 hard fail，1.0–1.5 秒本地记录 advisory，发布模式按当前 hash 复核；禁止添加噪声规避。继续保留真实解码、媒体结构、音轨一致性、固定 ASR、响度与 true peak 检查。preparer PASS 后不默认重复全量 verifier。

[历史制作记录](tools/video/research/legacy-production-notes.md) 仅在相关故障时查阅，旧案例参数不作为新项目约束。
