# CONVENTIONS.md — 制作与编辑规范

本文件维护制作默认和编辑验收目标。代理边界见 [AGENTS.md](AGENTS.md)，可执行步骤与 manifest 字段见 [Runbook](tools/video/README.md)。用户当期要求可覆盖制作偏好；技术门禁不因审美选择降级。

## 测试产物与生产产物

目录表示项目用途，版权与发布审核按以下范围执行：

| 目录 | 产物定位 | 版权与发布审核 |
| --- | --- | --- |
| `sandbox/<slug>/` | 全部视为内部测试产物，包括完整成片、封面和配套发布文案 | 不纳入公开发布的版权风险审核，不要求发布授权证明，不启用发布级真人门禁；按测试 brief 完成实际素材制作与本地验收 |
| `production/<slug>/` | 正式产物及其需长期保留的可复现工程 | 核对素材授权范围、使用方式和目标发布平台的风险；公开发布前完成适用的发布验收 |

- sandbox 制作不因“可能有版权”而停止搜寻、弃用可用 MV、缩短完整乐段或改成专辑封面、静图、占位动画。选材继续按真实身份、版本、画面内容与可用性判断；无 MV 或下载失败时记录具体证据，不能虚称被版权检测拦截。
- `publishing/xiaohongshu.md` 是配套文案文件名；本地 FINAL PASS 是技术验收结果。两者都不会把 sandbox 自动变成公开发布任务。
- 用户要求将测试片公开发布或按可发布成品交付时，先将项目转入 production 发布流程，记录用途并核对授权。既有 production 存档也不代表已经发布或已获授权；普通维护不自动触发上传、重审全部历史工程或重新制作。
- production 的授权来源、允许的使用范围、目标平台及未解决事项记录在已有 `SOURCES.md` 或 `design.md`。不凭“官方来源”“能够下载”或技术门禁 PASS 推断已获发布许可；也不新增未受 schema 支持的 manifest 字段。可以独立完成的本地制作继续推进，发布所需的缺失输入按实际问题处理。
- 测试免审指不执行公开发布的版权审核，不对素材权利状态作无依据断言。来源真实性、表演版本、Cookie 只读边界、音画质量及当前 SHA 的技术检查在两类目录下均适用。平台实际拒绝访问时按错误恢复，不能把测试用途写成已获访问权限。

## 长期创作偏好

以下适用于每期视频的选材、封面、屏幕文字、旁白、叙事与发布文案，brief 未重复说明时仍执行。用户只需提供本期主题、想表达的重点和特殊要求，其余由模型自主判断并完成；当期明确要求可调整创作偏好，事实真实性仍须保证。

- **可信考证**：歌曲、访谈和文案依据从可信网络来源查找，优先官方发布、一手资料和可追溯的原始采访；实际打开原文或核对原片上下文。搜索摘要、转载标题和社区说法只作线索，不直接作为事实依据。入片的身份、版本、时间、引语及关键事实，在项目 `SOURCES.md` 或已有事实记录中保留来源链接、对应内容或时间码；采访中的个人说法保留归属，不升级为已证实事实。无法核实、来源冲突未解决的信息不进入成片或发布文案；主观赏析与事实区分，不编造经历、因果或引语。
- **创意服务内容**：封面美学与叙事节奏从本期主题、情绪和实际素材出发，主动寻找合适的新表达。用构图、文案、剪辑和节奏建立吸引力，控制装饰和信息密度；不为炫技堆特效、模拟按钮或无助理解的交互，也不为文艺感使用空泛隐喻。叙事铺垫、转折与留白按真实内容安排，音乐展示继续保留完整乐段与自然收尾。
- **文字通俗纯净**：大标题、小标题与信息卡可以有创意，但让普通观众一眼理解主题，避免生造概念、夸大断言和重复表达。中文为主，只保留帮助识别作品、人物或理解内容的文字；必要的外文原名、正式名称可保留，不堆砌仅作装饰的英文标签、编号或制作术语。屏幕信息与口播互补，保持清晰层级和阅读空间。
- **每期独立创作**：历史视频主要参考大致结构和可复用的工程方法，重新判断本期选题角度、章节逻辑、封面和视觉语言，不只换人物、歌单与标题。未提出排名需求时，不因旧工程方便而套倒数榜单；非排名按内容组织叙事。同一系列可以有依据地延续识别元素，不为求新强行换风格，也不设置差异配额。具体视觉自查见 [字幕与视觉](#字幕与视觉)。

在已有 `design.md` 中简述本期叙事角度与关键设计依据即可。文案定稿和成片抽查时，连同来源记录复核事实、文字必要性、实际节奏与历史参考的适用性；这是内部编辑自查，不增加用户审批，也不以机械门禁代替编辑判断。

## 配音

当前新旁白统一使用中央 `tools/tts/narrate.py` 的 **Qwen3-TTS** 能力，消费预先制作的参考声音。声音池、编号与决策画像分别以 `tools/tts/config.json`、`tools/tts/voices/registry.json` 为真源，不在规范重复列举。唯一精确指定优先；未唯一指定时由模型按作品情绪、叙事和节奏选择，低置信度才一次随机兜底。同一期选择固定，生成、检查和恢复见 [TTS README](tools/tts/README.md)。

只在生成新旁白时检查 TTS；复用历史 WAV 不自动换声。Kokoro 仅属显式历史复现路径，不是当前默认或故障降级方案。

## 素材与画幅

- 默认从 YouTube 和 B站各查候选，先核实目标歌手和表演版本。翻唱素材优先对应翻唱者，不用原唱画面冒充；真实缺口需记录原因。
- 身份正确后优先可用官方 MV，低清本身不是弃用理由。无可用官方 MV 才比较官方 Live、综艺或二次来源；同级比较干净度、立体声和画质。
- **质量接近时优先 YouTube**：比较两平台符合目标歌手、版本及上述来源优先原则的候选，综合考虑清晰度、音质、水印/版权 Logo 的数量与遮挡、内容完整性及 UP 主插入的无关内容。整体质量相近时优先选 YouTube；B站素材明显更好时选 B站，按实际画面和声音灵活判断。
- 用户明确指定链接、排除平台或素材确属平台独占时可免重复搜索。manifest 显式记录 search_exception，保留真实候选和选择证据。已有证据可复用，但不能把过期可用性当作当前事实。
- 成片固定竖屏 **1080×1920（9:16）**；只有用户明确要求其他画幅才调整，并在 manifest.output_format 记录尺寸与用户原话。不能仅凭 design 中的“保留全景”“更有电影感”自行改成横屏。展示画面默认全宽保原比例、模糊填充（letterbox）；复用 `vfill.sh`。横屏来源仍置于竖屏画布内。只裁水印/烧词的全宽窄横带。用户要求其他构图时检查完整候选窗，保证主体完整，多人不得被裁掉。
- 成片不出现平台/UP主水印、网址、提示词或本地路径。无法裁净时更换干净来源。

## 结构、旁白与音乐

固定 TOP brief 中，用户给定的曲数、歌名、明确指定的表演版本、名次，以及每首简短描述的核心意思均为制作约束，不自行增删、换歌、重排或改写评价方向。描述可为口播通顺做不改变原意的精简润色；用户要求逐字保留时照录。创意用于封面、视觉、转场与呈现，不以“独立创作”或“去模板化”为由改成非排名叙事。描述涉及事实仍须核验；有冲突时标明具体问题，必要时询问，不擅自替换用户结论。文案定稿与交付前，逐项对照原始 brief 核对歌单、名次和描述原意，自动排名检查不替代这项核对。

TOP 默认按 N→1 逐首揭晓；封面和 intro 不泄第一名、完整歌单或内部倒数机制。非排名按叙事顺序，不挂 TOP 名义。信息标识避免同义重复，展示段默认淡出排名/歌名浮层。

默认结构是开头、逐首短转场、作品收束和 CTA。需要不同结构时在 manifest 的 editorial 中声明选择和 brief/design 理由，不伪装成自由探索。intro 建立主题，outro 收束；转场优先短而具体，句数与判断数量由文案决定。开头直接进入主题，避免空泛串场语，不设禁词表。

新项目 schema v2 的转场 WAV 仍执行：TOP 硬上限 8 秒（目标 4–6），叙事硬上限 10 秒（目标 6–8）。超限先精简旁白，不靠延长歌曲刷比例。其他自定义段按实际结构组织，不能改 role 规避真实歌曲转场的时长检查。时长门禁只是上限；转场和赏析还要按所选声线的实际语速判断节奏，在保留原意和关键听点的前提下删去重复解释与串场语，不靠加速配音解决文案冗长。

CTA 执行下文[结尾引流配音](#结尾引流配音)要求。章节时长根据实际 WAV 和乐句计算，不照抄旧常量。

音乐展示先选完整段落，再计算时长。“短视频”只描述定位，不隐含 3–4 分钟或任何总长上限，也不按曲数平均分配秒数。每首保留完整连续副歌或代表性核心段落，必要时连同铺垫、回应句、第二轮副歌或器乐发展一起保留；一句唱完不等于整个段落已经完整。初步找窗可先预留约 60 秒的纯音乐展示空间，再按作品伸缩；这不是硬下限或上限，完整短段可以更短，长段超过一分钟也不因此缩短。展示时长不包含旁白和 duck 时间，不用加长口播填总时长。

取材时在拟选核心段前后各保留至少 3–5 秒可回调素材（源边界不足时如实记录），先核对带上下文的候选窗，再确定成片切点。满音量展示通常从首个目标唱字/音符前约 1–2 秒开始，末个唱字、长音与混响实际结束后再留约 1–3 秒音乐余韵；按真实节奏调整，不能为了凑缓冲拖进下一句后再切断，也不填人工静音。旁白应在核心入点之前收完，full-music 展示开始后没有旁白。淡出放在完整尾音之后，不能用淡出掩盖半句截断。纯音乐同样保留完整主题或乐段的发展与解决。

逐首在 design/选段表记录核心段源起止、最终展示起止及选择理由。试听入点前后、出点前后至少各 5 秒上下文；mux 后再听每个实际交接，核对首字、尾音、淡出和下一首衔接。自动边界检测只证明它覆盖的证据，不能把低能量气口当作完整段落证明。有用户硬时长时先精简重复信息，仍无法兼顾完整段落与锁定歌单才询问取舍，不强截音乐。

旁白期间音乐 duck，留适当消化位再恢复全量。通常以约 25% 音乐增益和 0.8–1.2 秒过渡为起点，按实际听感调整。预混 `master.wav` 后期 mux，最终响度、true peak 和静音以中央 FINAL 为准；不铺噪声或无关音乐绕过检测。

配音分段不自动成为背景音乐的重播点。连续收束（如结尾总结至 CTA）默认共用一条持续推进的音乐，跨段保持源时间连续，到作品结束再自然淡出；只有明确的叙事需要才重新起奏。

歌曲音频在开头、主题切换及收尾的切入切出，按乐句、节奏和情绪安排淡入、淡出或短交叉淡化；硬切应有明确的节拍或叙事依据。过渡包络作用于音乐层，旁白独立保持完整清晰，避免对整段混音淡出而吞掉配音尾字。

## 结尾引流配音

- **必须保留**：有旁白的盘点、解说与主题叙事成片，在作品内容收束后追加一句简短的引流配音，明确邀请观众点赞、收藏、关注。它是全片最后一句旁白，只出现一次；brief 未提及仍须自动补齐。主题问句只能作为可选的内容互动，不能替代引流句，也不必每期强行提问。
- **统一短句**：新项目逐字使用 `tools/video/outro_cta.py::FIXED_OUTRO_CTA`（`short-v1`），通过 `python3 tools/video/outro_cta.py` 取得。只保留一句自然邀请，不附加投票问题、长篇下期预告或催促话术；自然语速目标约 3–6 秒，以实际 WAV 为准，不加速挤时长。
- **例外由用户决定**：只有用户明确要求替换、省略 CTA 或全片无旁白时才调整，在 `editorial.cta_user_request` 记录用户原话并填写 reason。模型不得以“非排名”“纪录片气质”“去模板化”“避免通用话术”或已有主题问句自行取消。真实自由探索与 AI 音色 MV 仍按各自声音方案，不得借项目类型规避普通叙事的要求。
- **实际入片**：CTA 与本期共用已解析声线，独立登记 text/WAV/sidecar，作为最后一条 `outro_cta` 绑定时间轴；音乐适当 duck，念完保留自然收尾，不截尾字。可配简洁引导浮层，但画面文字、发布文案和静音 WAV 都不能代替引流配音。
- **验收闭环**：PROJECT 校验 CTA 文本、唯一末位及真实 WAV/sidecar；FINAL 继续检查最终 MP4 的音轨绑定和对应 ASR 覆盖，并实际核对片尾完整、清晰可听。修改片尾须重建受影响的旁白、音轨、画面及 mux，刷新当前成片 SHA 的 QA。历史长版仅供已有项目按 Runbook 显式固定版本后复现，新制作使用短版。

## 字幕与视觉

默认不添加自定义旁白同步字幕；用户明确要求字幕才添加。信息卡、歌名、主题和 CTA 浮层是设计信息，避免逐字复制口播。

每期视觉从作品主题、音乐气质、时代背景和实际素材出发，独立决定封面构图、配色、字体与运动方式。在 design.md 简述希望观众感受到什么，以及关键视觉选择如何支持本期表达；仅写“怀旧”“高级”“纪录片感”等通用风格词不足以说明选择依据。

装饰数量、easing、镜头切换方式由模型根据作品决定。不要求暗底、三种 easing、8–10 个元素、持续装饰运动、纯色禁令或强制 prompt 扩写。Flex、Grid、绝对定位和手动语义换行均可，按实际画面验收。

- 历史作品和样式示例只作参考，不自动成为新作品的视觉默认。可以复用时间线、素材处理与渲染代码，封面构图、配色和字体仍按本期内容重新判断；同一作品内保持协调，同一系列可有依据地延续视觉识别。复用 TOP 排名结构不代表沿用旧片的文字位置、字号和留白，技术门禁通过也不代表其版式值得复用。
- 封面定稿时，若近期作品首帧方便取得，可并排查看最近几期，检查是否只替换人物和标题。明显趋同时复核本期内容或系列定位的依据；允许有理由的相似，不强制轮换颜色、字体或达到差异配额。此项是轻量内部自查，不增加审批或阻塞制作的历史素材搜集。
- 第 0 帧完整可读，可直接作封面；首屏文字不从透明开始。后续是否入场/退场/硬切按内容选择。
- 第一首动态素材连续流入正文是可选优先方案，也可以使用更合适的静帧或独立封面，不要求证明其他方案穷尽。
- 单歌手主题须让歌手身份突出；不要求其字号严格大于所有其他文字。按主题与负空间确定视觉焦点。
- 按整帧检查视觉重心与文字实际占宽。短文字集中在一侧、另一侧缺少主体呼应或明确留白作用时，优先调整文字组位置、宽度、换行和字号；可整体居中、组内左对齐，不靠补装饰填空，也不强制所有画面居中。左对齐本身不是问题，失衡的排布不能仅用“极简”解释。
- 语义自然断行，不留下孤字、不挡脸或主要动作。关键文字集中在安全区域；1080×1920 可参考 x=72–1008、y=220–1420，其他画幅按目标裁剪重新设计。
- 封面已有“最难”等明确榜单语义时，默认省略重复 TOP 角标。其余信息去重按实际画面判断。
- 以首帧、代表时刻和目标平台裁剪预览检查完整可读、主体避让、色彩与层级；TOP 另覆盖长短歌名转场卡和片尾，缩到手机观看大小复核，必要信息不应依赖放大才能读清。元素数量和 CSS 写法不是质量证明。
- 新项目使用项目内 WOFF2。离线要求不限定字体家族，已有字体按本期表达与可读性选择，不因前几期用过就默认沿用。历史本机专用工程可继续已记录的 `local()`，但不能据此宣称跨机可复现。渲染不依赖远程字体请求。

### 封面案例参考

凡涉及封面设计（新建、重做、优化或复用工程），先读 [封面案例库](references/covers/README.md) 的使用说明、设计标准与索引，再按本期需要查看相关案例的定稿图、提示词和设计简述。

- **作品表达优先**：先说明本期讲什么、希望观众感受到什么、首屏应先看见什么，再决定案例是否有帮助。案例仅作辅助参考，不代表统一风格或必须复刻的模板；不照搬整张构图、配色、字体、人物位置或提示词，也不只换标题和歌手。
- **借鉴设计理由**：提炼与本期有关的信息层级、主体关系、留白、文字与图像协作、移动端可读性等方法，再为本期独立设计。没有合适案例时直接自主创作，不凑参考数量、不强求视觉相似，也不增加用户审批。
- **轻量记录取舍**：在本期 `design.md` 简记参考案例及借鉴点、本期如何调整；未采用案例时说明本期自身的设计依据即可。案例里的尺寸、字号、颜色与版式参数只解释该案例，不升级为全局硬约束。
- **案例与实际验收分开**：参考案例不免除下文 AIGC 生图要求、人物与来源核对、首帧及目标裁剪检查。入选代表有值得学习的设计，不等于适用于所有作品，也不是点击率、真人听审或发布授权的证明。仅归档已有定稿不属于新建、重做或优化封面，无需为入库重新生图或重渲成片。

新增案例按案例库 README 的目录结构保存独立图片、原始提示词、设计简述及来源/版本记录，并更新索引。不要把普通产物批量搬入，也不要因单个案例调整全局风格默认。

### 封面图像生成流程

- **作为必经步骤**：新建、重做或优化封面时，必须实际调用 AIGC 图像生成或编辑模型，并将经检查的结果用于封面。不能因已有素材够用、排版已完成或沿用旧封面而省略，也不以仅检查工具或生成后未采用作为完成。
- **按能力选择工具**：优先使用 imagegen；该入口不存在或不可调用时，检查当前 agent 提供的其他生图/图像编辑工具，确认其实际调用 AIGC 图像模型后即可使用，不限定工具名称、厂商或宿主。依据当前工具说明核实能力，技能文件存在或过去调用成功不等于当前工具可用；仅缺少名为 imagegen 的工具不构成跳过理由。
- **严禁代码绘图替代生图**：封面新生成的主视觉、背景、插画等图像必须来自 AIGC 图像模型。不得使用 SVG、HTML/CSS、Canvas、Python 绘图库等程序绘制替代图像；由语言模型编写绘图代码、网页截图或矢量转 PNG 均不算模型生图。已有图像的裁剪、缩放、合成及标题文字排版可使用现有制作工具，但不能借此绘制替代的封面图像。
- **唯一跳过条件是所有 AIGC 生图入口均不可用**：只有检查当前 agent 的实际工具后，确认所有相关入口都因工具缺失、服务或权限等环境故障无法调用，才可记录环境例外。调用失败先区分环境故障与可修正的输入、提示词问题；有恢复路径时作合理重试，不能把一次失败或画面效果不理想直接当作不可用。确认后，在 `design.md` 或已有素材记录中简记已检查的入口、实际错误及跳过原因，明确本轮未执行 AIGC 生图；可使用现有真实图像和必要文字排版继续完成封面，仍严禁代码绘图补位，不为恢复工具强制安装依赖、索取凭据或阻塞交付。
- **灵活选择用途，持续优化成稿**：根据作品主题与实际素材，选择背景延展、边缘柔化与底色融合、氛围图像或构图优化等用途，不沿用固定风格。可以保留原始人物或专辑图作为主体，结合生成背景及原生文字排版；不要求整张封面或所有元素都由模型生成。效果不理想时调整提示词、参考图或布局后继续优化，实际成稿的吸引力与可读性仍是判断依据。
- **保留来源并检查实际封面**：编辑现有封面或人物图前先查看原图，在提示词中说明要改变和要保留的内容；区分原始素材与生成衍生图，不将生成细节当作史料或官方原图。记录实际工具、可取得的模型标识、提示词及输入/输出对应关系，不能把其他工具的产物记作 imagegen。采用结果须与参考原图对照，检查人物身份、形体和关键细节，再在实际排版、首帧及手机尺寸下检查主体、文字、裁剪和色彩融合；生成成功不等于设计完成。最终素材、采用版本的提示词和来源记录按 [Runbook](tools/video/README.md#1-brief-与准备) 保存在项目内。

## 发布文案

默认随片交付 publishing/xiaohongshu.md，结合真实主题、事实与本期角度写成可直接发布的内容。可用已有来源研究，不为补文案机械重复调研。

标题候选 1–5 个，默认 3 个，第一条首选；正文最后一行放相关且不重复的 hashtags。420–900 字、8–10 个标签是参考范围，不是硬门禁；段落和互动问题按内容决定，不凑字数和标签。默认不用 emoji。

避免明确曝光本期歌名；《歌名》和精确歌名 hashtag 属明确引用。普通词语恰好与歌名相同（如“后来才发现”）由编辑看上下文，不直接判剧透。校验器将歧义和长度提示为 EDITORIAL，不证明文案事实或审美已通过。

## QA 与恢复

构建前 PROJECT 验证来源、当前片段、旁白及展示证据，mux 后 PUBLISHING 和 FINAL 验证交付。不能用自报 PASS 代替中央工具。技术细节集中在 [Runbook](tools/video/README.md) 和 [运行与恢复](tools/video/operations.md)。

展示检测先使用 multi。REVIEW 按原因处理：可修复识别问题则重跑分析；窗口有问题则换窗；素材有问题则换同版本源。Live 分类器本身不能自动 OK 时，不要求重复尝试必定 REVIEW 的窗口；结合原源、歌词、口型和音频证据作当前 hash/窗口绑定的 agent observation，状态只能是 OBSERVED。硬 FAIL 必须修复。发布模式只接受真实 human 批准。

标准 FINAL 中 >1.5 秒连续静音始终 hard fail，1.0–1.5 秒本地记录 advisory，发布模式按当前 hash 复核；禁止添加噪声规避。继续保留真实解码、媒体结构、音轨一致性、固定 ASR、响度与 true peak 检查。preparer PASS 后不默认重复全量 verifier。

[历史制作记录](tools/video/research/legacy-production-notes.md) 仅在相关故障时查阅，旧案例参数不作为新项目约束。
