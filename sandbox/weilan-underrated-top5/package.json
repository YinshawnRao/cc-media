{
  "private": true,
  "scripts": {
    "narrate": "python3 build/narrate_segments.py",
    "build": "python3 build/full_build.py",
    "lint": "npx hyperframes lint",
    "inspect": "npx hyperframes inspect",
    "render": "npx hyperframes render --output renders/full_raw.mp4 --sdr",
    "mux": "ffmpeg -y -i renders/full_raw.mp4 -i master.wav -map 0:v -map 1:a -c:v copy -c:a aac -b:a 192k -shortest renders/weilan-underrated-top5.mp4"
  }
}
