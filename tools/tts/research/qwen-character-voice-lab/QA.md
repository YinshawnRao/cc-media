# 首轮 QA 结果

日期：2026-08-09
状态：`AUTOMATED QA PASSED — 用户已选择 CV002「治愈少女」作为工作区默认；其余角色与混合语言仍可继续人工横评`

## 产物

- Qwen3-TTS：8 条原创 VoiceDesign 母带。
- Qwen3-TTS：每条母带各生成 1 条盘点样音和 1 条中英日混读，共 16 条 Base 克隆样音。
- Kokoro：当前仓库全部 8 个中文预置音色的同文案基线。
- 公平试听副本：32 条，统一为 24kHz 单声道 PCM WAV，并以 `-18 LUFS / -1.5 dBTP` 为目标做响度匹配。
- 本地完整实验试听页可由脚本生成到 `outputs/listen.html`（`outputs/` 不入库）；仓库内精选、可直接打开的正式试听入口为 `../../voices/listen.html`。

## Qwen 本机性能

环境：Apple Silicon + MLX-Audio 0.4.5，使用参考项目中已下载并固定 revision 的 8-bit 权重。

- VoiceDesign 母带：平均 `2.91s`，范围 `2.45–3.73s`。
- Base 克隆 8–10 秒样音：平均 `2.90s`，范围 `2.24–3.53s`。
- 报告中的最高峰值内存：约 `7.72GB`。
- 同一 manifest 立即复跑：8 条母带与 16 条样音全部通过 SHA-256 + 参数指纹校验命中缓存，没有重复生成。

原始证据：`qa/qwen-generation-report.json` 及每个 persona 目录内的 `*-meta.json`。

## 音频技术检查

`qa/audio-report.json` 共检查 88 个 WAV（raw、ready、统一响度试听副本）：

- 可解码：`88/88`
- 技术状态 PASS：`88/88`
- FAIL：`0`
- 警告：28 个，主要来自原始文件或母带中的自然句间停顿；统一试听副本中 7 条检测到超过 0.8 秒的停顿，未发现文件截断。

特别说明：第一版试听派生滤镜曾把句内停顿误判成结尾，自动 QA 检出 21 条时长不足 1 秒；已改为“正向只裁开头 + 反向只裁结尾”并全部重建。上述 `88/88` 是修复后的新报告。

## ASR 内容证据

使用仓库现有本地 Whisper `small`，对 8 条 Qwen 盘点样音、8 条 Qwen 混合语言样音和 8 条 Kokoro 盘点基线共检查 24 条。

- Qwen 8 条普通中文盘点样音：规范化相似度 `0.975–1.000`，核心文案均完整。
- Kokoro 7/8 条：`0.925–0.975`。
- Kokoro `zf_xiaoni`：`0.475`，Whisper 出现大量错字，必须人工重点试听；这可能是实际清晰度问题，也可能是高音色导致的 ASR 偏差，不能只凭 ASR 判死刑。
- Qwen 中英日混读：原始转录已保留，但 Whisper 在日文作品名上常输出近音字；只能作为证据，不能替代耳听发音判断。
- 相似度只把本轮实际观察到的简繁体差异做规范化，原始转录未改写。

原始证据：`qa/asr-report.json`。

## 已完成与仍待确认的人工门禁

- 用户已试听并明确选择 `CV002「治愈少女」` 作为后续新盘点默认，已提升到 `tools/tts/config.json` 与正式编号声音库。
- “角色辨识度、萌感、自然度、长听疲劳、母带到克隆的一致性”无法由自动工具判断。
- 中英日混读必须逐条人工确认，尤其《進撃の巨人》的日语读法。
- 当前仅为本地非商业测试；进入未来公开/商业生产前，还要固定许可证快照并重新做声音权利审计。
